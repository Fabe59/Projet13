language: python

python:
  - '3.6'

addons:
  chrome: stable

install:
  - pip install pipenv
  - pipenv install --dev

env:
  global:
    - SECRET_KEY = "7q+i=7c%v)-bwudx&^+p7ltj@c30he1&@i9ggfd3ls26r!isim"
    - DJANGO_SETTINGS_MODULE="openrider.settings"

services:
  - postgresql

script:
  - cd openrider && python manage.py test -v2